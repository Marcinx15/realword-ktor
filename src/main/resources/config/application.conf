ktor {
  host = "0.0.0.0"
  port = 8080
}

db {
  url = "jdbc:postgresql://localhost:5432/real_world"
  username = "user"
  password = ${POSTGRES_USER_PASSWROD}
}

jwt {
  audience = "realword-ktor-api"
  issuer = "realword-ktor"
  expiration = 30.minutes
  secret = ${JWT_SECRET}
}